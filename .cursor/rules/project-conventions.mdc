---
description: Project-specific conventions and lessons learned
alwaysApply: true
---

# EZstream Project Conventions

## Shell Commands (PowerShell)

This project uses PowerShell on Windows. **Do NOT use bash syntax:**

```powershell
# ❌ BAD - && is not valid in PowerShell
git add -A && git commit -m "message"

# ✅ GOOD - Run commands separately
git add -A
git commit -m "message"
```

## ESLint Disable Comments

The server may not have all TypeScript ESLint plugins. **Avoid TypeScript-specific ESLint rules:**

```typescript
// ❌ BAD - Rule may not exist on server
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const handler = (results: any[]) => { ... }

// ✅ GOOD - Use unknown type instead (no disable needed)
const handler = (results: unknown[]) => {
  setResults(results as MyType[]);
}
```

## Type Compatibility with Callbacks

When passing callbacks to components, ensure type compatibility:

```typescript
// ❌ BAD - TVShow[] not assignable to SearchResult[]
const handleLiveResults = useCallback((results: TVShow[]) => {
  setSearchResults(results);
}, []);

// ✅ GOOD - Use unknown[] and cast inside
const handleLiveResults = useCallback((results: unknown[]) => {
  setSearchResults(results as TVShow[]);
}, []);
```

## Mobile Safari Iframe Considerations

**Do NOT use sandbox attribute on video embed iframes** - it breaks playback:

```tsx
// ❌ BAD - Breaks video embeds
<iframe sandbox="allow-scripts allow-same-origin" ... />

// ✅ GOOD - No sandbox for video embeds
<iframe allowFullScreen allow="autoplay; encrypted-media" ... />
```

## Mobile Touch Handling

Use CSS media queries for touch detection, not JavaScript:

```css
/* ✅ GOOD - Instant detection, no hydration delay */
@media (hover: none), (pointer: coarse) {
  .card-scale:hover { transform: none; }
  .card-action-btn { opacity: 1 !important; }
}
```
